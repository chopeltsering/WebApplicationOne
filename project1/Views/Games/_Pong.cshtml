<script>
    var width = 700, height = 600, pi = Math.PI;
    var canvas, ctx, keystates;
    var player, ai, ball;

    function main() {
        canvas = document.createElement("canvas");
        canvas.width = width;
        canvas.height = height;
        ctx = convas.getContext('2d');
    }
</script>